package com.example.nouveauProjet.repositories;


import com.example.nouveauProjet.entities.Role;
import com.example.nouveauProjet.entities.Utilisateur;
import jdk.jshell.execution.Util;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;
/*

@Repository

public interface UtilisateurRepository extends JpaRepository<Utilisateur,Long> {

    Boolean existsByCode(Long code);
    Boolean existsByEmail(String email);

    Boolean existsUtilisateurByTelephone(String telephone);

    Utilisateur findByEmailOOrTelephone(String telephone);

    Utilisateur findByEmail(String email);

    List<Utilisateur> findAllByRolesIn(List<Role> roles);
}
*/
